# David Mytton

This is my website and blog! It's generated using [Hugo](https://gohugo.io) with
the [Congo](https://jpanther.github.io/congo/) theme.

## Deployment

Use `hugo -D --gc --debug` as the build command to provide debug logs and clean
up resources that have been cached, but are no longer in use.
